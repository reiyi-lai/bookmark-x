{
  "name": "bookmark-x-extension",
  "version": "1.0.0",
  "description": "Chrome extension for Bookmark-X",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run copy-static",
    "preview": "vite preview",
    "copy-static": "cp manifest.json dist/ && cp -r icons dist/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3",
    "vite": "^5.0.7"
  }
} 